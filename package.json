{
  "scripts": {
    "migrate:check": "db-migrate check",
    "migrate:up": "db-migrate up",
    "migrate:down": "db-migrate down",
    "docker:up": " docker compose up -d",
    "docker:up:rebuild": " docker compose up --build -d",
    "docker:down": " docker compose down"
  },
  "dependencies": {
    "sequelize": "^6.37.5"
  },
  "devDependencies": {
    "db-migrate": "^0.11.14",
    "db-migrate-pg": "^1.5.2"
  }
}
